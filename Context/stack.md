# Project Stack

_Maintained by `/setup`. Re-run `/setup --re-detect` to refresh._

## Detected stacks

- `kotlin-android` — Kotlin / Android (app/, wear/, domain/, data/, core/, ui/, feature/, dashboard/, sync/, notification/)
- `rust` — Rust (generic) (planned — backend service for touchscreen dashboard; not yet scaffolded)

## Unrecognized stacks

- Slint — declarative UI toolkit for the Rust touchscreen dashboard. No `core/rules/` slot in Cortex; populate `.claude/rules/slint/` manually when the dashboard work starts.

## Detection metadata

- Detected: 2026-04-22T00:00:00Z
- User-confirmed: yes
- Greenfield fallback: no
